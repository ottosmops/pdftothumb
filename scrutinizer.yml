build:
    dependencies:
        before:
            - sudo apt-get install -y poppler-utils
    environment:
        php:
            version: 7.1

filter:
    excluded_paths:
        - 'tests/*'
checks:
    php: true
coding_style:
    php: {  }
